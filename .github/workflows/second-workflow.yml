name: second-workflow
on: push
jobs:
    using_actions-workflow:
      uses: sahil4010/actions-pipeline/.github/workflows/reusable.yml@test
    
    verify_multiple_call:
        runs-on: ubuntu-latest
        needs: using_actions-workflow
        steps:
        -   name: check
            run: echo "hello"
      
      
